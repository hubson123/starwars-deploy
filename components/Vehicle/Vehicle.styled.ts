import { styled } from "styled-components";
import { StyledCard } from "../../styled-components/General";

export const VehicleCard = styled(StyledCard)`
  margin-top: 2rem;
  flex-direction: column;
  border: 3px dashed yellow;
`;
